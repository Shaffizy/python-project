import math
height = float(input("What is the height: "))
radius = float(input("What is the base: "))
volume_of_cylinder = math.pi*radius**2*height
print(F"the volume of the sphere is {volume_of_cylinder}")
