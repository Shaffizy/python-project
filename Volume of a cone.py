import math
height = float(input("What is the height: "))
radius = float(input("What is the base: "))
volume_of_cone = math.pi*(1/3)*radius**2*height
print(f"the volume of the cone is {volume_of_cone}")
